# First Join - Artist piece

- Include adding the join to the `artwork` piece.
  - Note on reverse join and link to guide to schema
  - Uncomment or add the `artistId` piece of `artwork`'s `importBeforeInsert`.